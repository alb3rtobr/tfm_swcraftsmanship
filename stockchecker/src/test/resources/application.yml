spring:
  application:
    name: stockchecker
  profiles:
    active: dev
stockchecker:
  threshold: 1
dal:
  host: localhost
  port: 50051
kafka:
  bootstrap-servers: ${spring.embedded.kafka.brokers}
  topic:
    json: mytopic