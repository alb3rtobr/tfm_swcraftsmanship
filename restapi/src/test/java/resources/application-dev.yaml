spring:
  profiles:
    active: dev

server:
  port: 8080

kafka:
  bootstrap-servers: localhost
  topic:
    receiver: receiver.t

dal:
  host: localhost
  port: 50051

logging:
  level:
    org.springframework: DEBUG
    com.craftsmanship.tfm: DEBUG
