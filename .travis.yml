language: java

jobs:
  include:
    - stage: test restapi
      script: cd proto-idls;mvn clean install;cd ..;cd restapi;mvn test
    - stage: test stockchecker
      script: cd proto-idls;mvn clean install;cd ..;cd stockchecker;mvn test


#script: "./ci_tests.sh"
